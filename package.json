{
  "name": "node-fetch-timeout",
  "version": "1.0.0",
  "description": "Wrapper package for node-fetch package that adds timeout and retry option.",
  "main": "lib/index.js",
  "scripts": {
    "start": "node lib/index.js",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "author": "Karan Raina <karanraina1996@gmail.com>",
  "license": "ISC",
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.16.5",
    "@babel/preset-env": "^7.16.5",
    "babel-polyfill": "^6.26.0"
  },
  "dependencies": {
    "node-fetch": "^2.6.6"
  }
}
